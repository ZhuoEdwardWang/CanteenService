[build]
  publish = "public"
  functions = "netlify/functions"

[functions."push-lunch"]
  # Netlify cron uses UTC. 03:00 UTC = 11:00 Asia/Shanghai.
  schedule = "00 3 * * 1-5"

[functions."push-supper"]
  # Netlify cron uses UTC. 09:00 UTC = 17:00 Asia/Shanghai.
  schedule = "00 9 * * 1-5"
